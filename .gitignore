# ---------- 依赖 ----------
node_modules/

# ---------- 构建产物 ----------
md-reader/dist/
# 打包前 `mv dist` 留下的临时备份（每轮一个，跑久了会堆积）
md-reader/dist.bak.*/
md-reader/src-tauri/target/

# ---------- 安装包 ----------
# 体积大且可由源码重新构建，只留最新版在根目录，历史版本放「历史安装包/」
*.exe

# ---------- 自动生成的测试素材 ----------
# 由 `npm run fixtures` 确定性重新生成（固定种子），不必入库。
# 手工编写的小样例（GFM 样例、GBK 编码样例、sample-image.svg）必须保留。
测试样例/大文档-5MB.md
测试样例/大图-10MB.png

# ---------- 工具数据 ----------
# WorkBuddy 的会话记忆与工作区配置，与项目源码无关
.workbuddy/

# ---------- 杂项 ----------
*.log
.DS_Store
Thumbs.db

# MD 阅读器 v0.2.12 — 装好以后怎么试

> **这一版做了一处小调整：把「复制代码 / 复制表格」按钮挪到代码块的右上角，不会再挡到正文。**
> 你**不装也不影响使用**；觉得按钮碍眼可以顺手装一下。
>
> 装机实测 8 项至此**全部闭环**（SVG / 大图也确认正常了）。这是首个**计划公开发布**的版本。

## 〇·三、这一版做了什么

1. **复制按钮顶位** —— 之前按钮虽然 CSS 写在 `top: 6px; right: 6px;`，但 pre 的 `padding-top` 只有 12px，按钮自身 22px 高，于是下半截和第一行代码重叠（特别是只有一行的代码块，看起来像按钮「挤进」了行里）。这一版把 `padding-top` 抬到 26px，按钮永远落在文字上方的独立区域；边框也加深了，不再和字符糊在一起。
2. **8 项装机问题全闭环** —— 你确认 SVG / 大图都正常了，连同上版已修的 6 项，这一版没有任何未结的 bug。**v0.2.12 是首个计划公开发布的版本**，下面的 GitHub 发布流程按这个版本走。

**回归验证**：39 项断言全绿，GFM 官方规范集 7/7 通过（449/449 基线无回归）。

## 〇、病根找到了：React 在反复重写整篇正文

你上一份诊断报告里，那行调用栈是决定性的：

```
写入来源：innerHTML= ← at Xt ← at HTMLElement.set ← at At ← at Qb
```

`At` / `Qb` 是 React 的内部函数 —— **是 React 自己在反复重写正文，一共重写了 13 次。**

顺着这条线查到 react-dom 19.2.8 的源码，两个细节凑在一起就是病根：

| React 内部 | 实际行为 |
|---|---|
| 判断某个属性变没变 | 用**对象身份**比较（`nextProp !== lastProp`），**不比内容** |
| 判定变了之后处理 `dangerouslySetInnerHTML` | **无条件**执行 `innerHTML = 新内容`，不再做任何比较 |

而我们原来是这么写的：

```jsx
<article dangerouslySetInnerHTML={{ __html: doc.html }} />
```

**花括号里的 `{...}` 每次渲染都会新建一个对象。** 于是 React 每次都认为"变了"，于是**每渲染一次，就把整篇正文销毁重建一遍**。

这一刀同时砍掉三样东西：

- 标题的 id → **大纲点了不跳**
- 命令式挂上去的复制按钮 → **按钮看不见**
- 查找结果里存的那批文本节点引用 → **不高亮，上下翻也不对**

而且每次重建都引发成片的页面重排 —— 报告里那 **213 次布局位移**就是这么来的，也就是你说的**滚到底部会抖**。滚动时每 50 毫秒要更新一次大纲高亮，就触发一次重渲染，就重建一次正文，文档高度瞬间塌陷、滚动条闪一下，固定定位的「回到顶部」按钮跟着横向一跳 —— 所以位移全记在 `button.back-to-top` 头上。

**修法只有一行**：用 `useMemo` 把这个对象的引用钉在正文内容上。内容没变，引用就不变，React 就会跳过。

本地已用自动化测试锁死：**内联写法重渲染 3 次就重写 3 次，`useMemo` 写法是 0 次。**

**v0.2.9 已验证：复制按钮、大纲跳转、查找高亮与上下翻都好了。**

## 〇·二、剩下的「GBK 滚到底部整个窗口抖」：滚动条震荡

你说"整个窗口都在抖动"—— **这个措辞是关键线索**。内容区内部重排不会让整个窗口抖，只有**可视区宽度本身在反复变化**才会有这种观感。

最典型的成因就是**滚动条震荡**，一个永不收敛的死循环：

```
正文略高于可视区 → 竖向滚动条出现 → 可用宽度少掉约 15px
   → 文字重排、总高度反而变矮 → 滚动条又不需要了、消失
   → 宽度涨回来 → 重排后高度又超了 → 滚动条再出现 → ……
```

每循环一次，**整篇正文就要完整重排一遍**。内容高度刚好卡在临界值附近时最容易触发 —— 所以往往只在特定文档、滚到特定位置才出现（这次是 GBK 样例的底部）。

**修法是一行标准属性**：给滚动容器加上 `scrollbar-gutter: stable`。滚动条无论出不出都占着同一个槽位，可用宽度恒定，上面那个循环从第一步就无从发生。

另外把窗口级滚动条也关掉了（`body { overflow: hidden }`）—— 它是第二个可能引起同样震荡的滚动条。

**这一版还加了一项检测「滚动容器宽度稳定性」**：盯着可用宽度，跳一次记一次。如果修好了，那一行应该显示「可用宽度一次都没变过」；如果还在跳，会直接写出在多少个像素之间跳、跳了几次 —— 差值接近 15px 就坐实是滚动条。

**另外还修了报告里的一个假警报**「查找痕迹」那行显示「Highlight API：不支持」是**错的**（同一份报告的上一行明明写着支持）。那是我把判定写在了搜索函数内部，你没搜索过就开面板，它还是初始值。已改成立即判定。

## 一、怎么装（**这一版必须先卸载旧版再装新版**）

文件关联沿用之前修好的实现，但**仍然必须先卸载旧版再装新版**——覆盖安装有时会跳过安装器钩子，导致注册表写入不生效。

**步骤：**

1. Windows 设置 → 应用 → 找到「MD 阅读器」→ 卸载。
2. 双击 **`MD阅读器-0.2.11-安装包.exe`**（就在这个文件旁边）。
3. Windows 可能会弹一个蓝色框，写着「Windows 已保护你的电脑」。
   这是因为程序**没有买代码签名证书**（一年几千块），所有个人和小团队做的软件第一次运行都会弹。
   点框里那行小字 **「仍要运行」** 就能继续。
4. 一路「下一步」，装完桌面和开始菜单都会有图标。

> 安装包只有约 2.5 MB，因为它不自带浏览器内核，用的是 Windows 系统里已有的 WebView2。

---

## 二、五分钟验收清单

打开程序后，用 `测试样例` 文件夹里的文件挨个试。每一项都写明了「应该看到什么」，对不上就是有问题。

| # | 试什么 | 怎么操作 | 应该看到什么 |
|---|---|---|---|
| 1 | 打开文档 | 点顶栏 **「打开文件」**，选 `测试样例/GFM 样例.md` | 正常渲染出标题、表格、代码、任务列表，**相对路径图片**也加载出来 |
| 2 | 打开文件夹 | 点顶栏 **「打开文件夹」**，选 `测试样例` 这一层 | 左侧栏切到「文件夹」tab，列出 `GFM 样例.md`、`GBK 编码样例.md`、`大文档-5MB.md` 等；当前打开的文件会被高亮 |
| 3 | 大纲目录 | 在侧栏点「大纲」tab | 列出这篇文档的所有标题，点一条正文跳过去 |
| 4 | 大纲显隐 | 按 `Ctrl + D` | 左侧栏收起 / 展开 |
| 5 | 页内查找 | 按 `Ctrl + F`，输入「表格」 | 顶部出现查找栏，**正文里匹配处变成半透明黄底**，显示「第 x 处，共 y 处」 |
| 6 | 查找跳转 | `Ctrl + G` 下一个，`Ctrl + Shift + G` 上一个，`Esc` 关闭 | 依次跳到每个匹配处，**当前那一个换成实底高亮**；关掉后高亮全部消失 |
| 7 | 换配色 | 顶栏「配色」下拉，四个都选一遍 | 经典 / 纸感 / 护眼 / 极夜，正文、代码、表格配色一起变 |
| 8 | 换明暗 | 点「深色」/「浅色」按钮 | 整体反色。4 套配色 × 明暗 = 8 种组合 |
| 9 | 换语言 | 顶栏「语言」切成 English | 界面文字变英文（文档内容当然不变）。关掉再开应该还记得你选的英文 |
| 10 | 阅读宽度 | 顶栏「宽度」下拉，选 80 / 60 | 文档区域变窄，居中显示，左右两侧留出空白。切回 100% 就铺满 |
| 11 | 代码高亮 | 在 GFM 样例里找代码块 | 关键字有颜色。滚到 Python / JavaScript / Rust 各看一眼 |
| 12 | 复制代码 | 找任意一个代码块 | 右上角**常驻**一个「复制代码」按钮，点一下按钮变「已复制」，剪贴板里就是这段代码（不带颜色 span） |
| 13 | 复制表格 | 找任意一个表格 | 右上角**常驻**一个「复制表格」按钮，点一下剪贴板里是这张表的 Markdown 源码 |
| 14 | 图片灯箱 | 打开 `测试样例/灯箱-10MB图.md`，点那张图 | 全屏放大。滚轮缩放、按住拖动、左右方向键换图、`Esc` 关闭 |
| 15 | 大文档 | 打开 `测试样例/大文档-5MB.md`（5 MB） | 能打开。来回滚动，看卡不卡 |
| 16 | 阅读进度 | 大文档里滚到中间，关掉程序，重新打开同一个文件 | 回到上次的位置。顶部有一条进度条 |
| 17 | 回到顶部 | 往下滚一段 | 右下角出现「回到顶部」按钮 |
| 18 | GBK 旧文档 | 打开 `测试样例/GBK 编码样例.md` | 能正常显示中文，标题栏文件名旁边标一个 `GBK` 小记号 |
| 19 | 外链 | 文档里点一个网址链接 | **用你的默认浏览器打开**，阅读器自己不跳转（这点是特意做的） |
| 20 | 任务列表 | GFM 样例里 `- [x]` 那几行 | 前面的小方框还在，打勾的显示打勾 |
| 21 | 裸网址 | 文档里写 `www.example.com`（没写 `http://`） | 也自动变成可点的链接 |
| 22 | **双击打开** | 在资源管理器里，双击任意一个 `.md` 或 `.markdown` 文件 | 直接用它打开（这一版**已修好**，见下面第四节） |
| 23 | **GBK 无抖动** | 打开 `测试样例/GBK 编码样例.md`，打开后**别动鼠标和键盘**，盯着页面看 5 秒 | 页面**稳定不跳**：正文、顶部进度条、右下角「回到顶部」按钮都不闪不晃（这一版修了 GBK 打开时页面抖动的 bug） |
| 24 | **阅读宽度铺满** | 顶栏「宽度」选 **100%**，然后把窗口拉到最宽 | 正文**跟着一起变宽、铺满整个内容区**。上一版的毛病是：无论选 60% 还是 100%，正文都卡在同一个宽度上不动 |
| 25 | **复制按钮看得见** | 打开 GFM 样例，随便找一个代码块或表格，**鼠标先别移上去** | 右上角**一直**有一个淡淡的「复制代码」/「复制表格」小按钮。上一版必须把鼠标移上去才浮现，很多人根本不知道有这个功能 |
| 26 | **一键诊断面板** | 按 `Ctrl + Shift + D` | 弹出自检面板，列出各项检查结果（详见**第七节**）。再按一次 `Ctrl + Shift + D` 关闭 |
| 27 | **大纲跳转**（本版主攻） | 打开 GFM 样例，在侧栏「大纲」里点任意一个标题 | **正文直接滚到那个标题的位置**。上一版的毛病是点了完全没反应 |
| 28 | **复制按钮**（本版主攻） | 打开 GFM 样例，数一数代码块和表格右上角的按钮 | 一共 **9 个**（6 个代码块 + 3 个表格）。上一版一个都没有 |
| 29 | **换文档后仍然有效** | 先开 `GBK 编码样例.md`，再开 `GFM 样例.md`，来回切两次 | 每次切换后按钮和大纲**都还在** |

---

## 二·五、一键诊断面板（`Ctrl + Shift + D`）

**到 v0.2.12 为止，装机实测报的 8 项已全部处理完毕。** 这个面板当初就是为了定位它们才做的 —— 有些行为只在真机上才暴露，我在自己这边猜不出来，得靠它把数据带回来。

现在它转为**日常排障工具**留着：以后再遇到说不清的现象，按一个键就能出一份报告，不用你懂技术、也不用开什么开发者工具。面板只做查询和计数，**不改变任何行为**。

所以做了这个面板：它把需要查的东西**自动测一遍、直接列出来**，你不用懂技术，也不用去开什么开发者工具。

**操作：**

1. 先用程序**打开一个有问题文档**（比如 `测试样例/GFM 样例.md`，里面有图、有表格、有代码块）。
2. **先做一遍会出问题的动作**：要查抖动，就把文档**从头滚到底、来回滚几下**；要查查找高亮，就先按 `Ctrl + F` 开查找栏、输入关键词并**保持查找栏开着**。
3. 按 **`Ctrl + Shift + D`**，弹出诊断面板，等一两秒让它测完（图片检测需要一点时间）。
4. 点面板底部的 **「复制诊断报告」** 按钮，然后**粘贴发给我**（微信、聊天窗口都行）。

> ⚠️ **第 2 步和第 3 步的顺序别反。** 抖动那一项是**累计值**，从程序启动就开始记；面板开了之后才滚动是记不到的。

**面板会告诉你什么：**

| 检查项 | 看到「异常」意味着什么 |
|---|---|
| 运行环境 | 你这台电脑的 WebView2 版本 |
| **初始化执行痕迹（决定性）** | **最重要的一行**。直接告诉你那段代码「跑没跑、跑到哪一步、装好之后有没有又被拆掉」，不用推理 |
| **页面抖动量化** | **查抖动靠这一行**。它是**累计值**——从程序启动就开始记。会写出「累计位移多少、一共几次、最大单次多少」以及**位移最多的三个元素**（比如 `div.markdown-body ×12`）。看到具体元素名，我就知道该去改谁 |
| **滚动容器宽度稳定性** | **查「整个窗口抖」靠这一行**。盯的是滚动条的槽位：可用宽度每变一次就记一次。显示「一次都没变过」＝正常；若在 `530px ~ 545px` 之间反复跳，且差值接近 15px，就是滚动条在反复出现/消失 |
| 当前文档统计 | 有几张图、几个标题、几个代码块/表格；**复制按钮挂上了几个**（和预期数量对不上就是有问题） |
| **DOM 一致性（判决用）** | **最关键的一项**。它会写出「有几个 .markdown-body」「标题里带 id 的有几个」「按钮宿主有几个」，靠这三项我能直接判断问题断在哪一环 |
| 查找高亮（CSS Custom Highlight API） | 显示「不支持」→ 那就是**查找不高亮**的原因，我得换成另一种做法 |
| 查找高亮现状 | `::highlight` 规则有没有注入、当前注册了几组高亮（**开着查找栏时应为 2**，没开查找时为 0） |
| **查找痕迹（跨会话）** | **查查找问题主要靠这一行**，而且**不用开着查找栏**——关掉之后再来开面板，它还记得上一次搜索的情况：命中了几处、上色执行了几次、最后注册了几个高亮、有没有因为正文被换掉而重算过 |
| 大纲标题 id 匹配 | 显示有几个对不上 → 那就是**点大纲不跳**的原因 |
| 阅读宽度 | 正文实际占了可用宽度的百分之多少（验证第 24 项） |
| 图片 1、2、3… | 每张图的 **Content-Type**（浏览器收到的数据类型），外加**实际收到了几个字节、开头那几个字符是什么**。看到「开头是 `<!DOCTYPE`」→ 说明拿到的是一个 HTML 页面，根本不是图片；写「仍在加载中」是还没传完，**不算失败** |

> **小提示**：面板里标「异常」的项目就是线索。全部复制发给我即可，不用你自己判断。

---

## 三、这一版还没做的

下面这些**不是坏了**，是排在后面的版本里，还没开工：

| 没做的 | 说明 |
|---|---|
| **把文件拖进窗口** | 拖放还没做 |
| **编辑文档** | 要做，但排在最后一批。现在只能看 |
| **多标签 / 分屏** | 一次只能开一篇文档 |
| **Mermaid 图表、数学公式** | 会做，排在下一批 |
| **宽表格「全部显示」** | 上一版想做，这一版决定不做（横向滚动条够用，且自动换行表格语义会被搞坏）。彻底取消 |
| **文件库侧栏、最近文件、收藏** | v0.3 |
| **导出 PDF / HTML** | v0.4 |
| **文件夹下钻（递归展开多层目录）** | 当前文件夹 tab 只显示一层。点文件夹不响应 |
| **大部分全局快捷键** | 现在只有 `Ctrl+F`、`Ctrl+G`、`Ctrl+D`、`Esc` 这四组 |

---

## 四、出问题了怎么办

| 现象 | 原因 / 怎么办 |
|---|---|
| 弹「Windows 已保护你的电脑」 | 正常，点「仍要运行」。见上面第一节 |
| 双击 .md 没打开它 | 多个可能，见下面「关于双击打开」 |
| 打开一份旧文档是乱码，还弹了提示 | 那份文件不是 UTF-8 编码。提示里有个 **「按 GBK 打开」** 按钮，点它。程序会记住这个选择，下次自动按 GBK 开 |
| 文档里的裸 HTML 显示不完整 | 白名单机制：危险内容（脚本、事件、iframe）会被剥掉，这是防恶意文档。排版用的 HTML 标签保留 |
| 图片不显示 | 图片路径是相对 md 文件所在目录找的。图片被挪走或删了就会显示占位框 |
| 切了宽度后文章还是不变 | 宽度是 CSS 变量驱动的，切档后立刻生效；如果只是窗口本身已经比设的宽度小，就退化成「按容器宽度显示」 |
| 代码块/表格的复制按钮怎么按不动 | 鼠标要先移到代码块/表格上按钮才浮现，移走就隐藏。这是防误触的设计 |

---

## 五、关于「双击 .md 直接打开」

**这一版我把上一版没做好的关联从根上重写了。**

**新版是怎么做的：**

上一版用的是 Tauri 模板自带的「APP_ASSOCIATE」宏写注册表——后来发现这个宏内部依赖一个叫 `SH`ELL_CONTEXT` 的变量，但 Tauri 模板从未定义它，结果 NSIS 编译器把变量名当成字符串写进注册表，写到一个名为 `SHELL_CONTEXT` 的假根键里——**静默失败，没有任何报错信息**，装完你以为装好了，双击 .md 还是没反应。

这一版做对的事：

1. 把 Tauri 模板的 fileAssociations 配置**移除**——别再用那个坏的宏。
2. 在安装器钩子（`installer-hooks.nsh`）里**自己手**写关联：用 `SHCTX`（跟随 `SetShellVarContext current`，HKCU 分支，不需要管理员权限、不弹 UAC）。
3. 同时注册「打开方式」列表——在 `HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\FileExts\.md\OpenWithProgids` 下加一个 REG_NONE 类型的登记，这样在「打开方式 → 选择其他应用」里也能看到本程序。
4. 写之前先备份原关联，卸载时还原——避免把原来指向 Typora / VSCode 的关联打乱。

**安装时为什么要先卸载旧版：**

旧版安装时虽然没真的写入关联（坏的宏失败），但**安装器在结束时会调 SHChangeNotify 通知资源管理器**——这本身不影响。但旧版的那次安装会留一份「上一次安装的痕迹」（比如安装目录），新版覆盖安装能识别它，但**部分 Windows 会跳过钩子**（因为它认为「没变化」）。先卸载确保注册表从 0 开始写，不会出现「上一版没装成功但状态机认定已装」的死锁。

---

**请装完之后做一件事验证：**

> 打开资源管理器，随便找一个 `.md` 文件，**双击**。

两种结果：

1. **直接用它打开了** → 完美，关联成功。试着再选另一个不同目录里的 `.md` 文件双击，也应该走它打开（关联是按扩展名生效的，与路径无关）。
2. **没反应，或还是用别的程序（Typora、VSCode 之类）打开的** → 这是 Windows 的「用户选择优先」机制在拦：如果你以前给 `.md` 设过默认程序，Windows 会优先用你当初的选择，任何软件都抢不走（这是微软故意设计的，防流氓软件）。解决办法：

   - 右键那个 `.md` 文件 → **打开方式** → **选择其他应用**
   - 在弹出的列表里找到 **MD 阅读器**，点一下
   - 勾选最下面那行 **「始终使用此应用打开 .md 文件」**
   - 点确定。以后再双击就直接用它了。

如果走「打开方式」这条路试过还是不行，请告诉我（操作系统版本、是直接没反应还是弹别的程序），我这边好定位是关联没生效还是别的什么。

---

## 六、关于这一版相比上一版新增的（按决策清单）

| 功能 | 怎么试 | 说明 |
|---|---|---|
| **打开文件夹** | 顶栏「打开文件夹」按钮 | 选一个目录，侧栏切到「文件夹」tab，列出其中所有 .md / .markdown / .txt 文件 |
| **阅读宽度** | 顶栏「宽度」下拉 | 60–100% 五档，默认 100%。窗口极窄时不会强制缩成宽度百分比 |
| **代码块复制按钮** | 鼠标移到代码块上 | 复制 `pre > code` 文本内容（不带高亮 span） |
| **表格复制按钮** | 鼠标移到表格上 | 复制 Markdown 源码，支持 align 属性 → `:---` 格式 |

### 上一版（v0.2.3）修好的 3 个 bug（这一版依然有效）

> v0.2.4、v0.2.5 都只升级了诊断面板；**v0.2.6 加了自动补回机制**（正文被换掉后重新补上），但实测没起效；**v0.2.7 改为直接取证**（拦截内容写入取调用栈 + 量化布局位移）。下面这 3 项是 v0.2.3 修的，装了这一版同样生效，可以顺手验一下。

| 修复 | 怎么试 | 说明 |
|---|---|---|
| **阅读宽度调不动** | 顶栏「宽度」选 100%，把窗口拉宽 | 上一版无论选 60% 还是 100%，正文都卡在同一个宽度上不动。根因是样式表里有一行把宽度写死了，把顶栏那个设置整个盖住。这一版改成由设置驱动，写死的那个值只留作兜底 |
| **复制按钮看不见** | 打开 GFM 样例，看任意代码块 / 表格的右上角 | 上一版按钮默认**完全透明**，必须把鼠标移到代码块上才浮现，很多人压根不知道有这功能。这一版改成**半透明常驻**——看得见但不抢眼，鼠标移上去变清楚。另外代码块的按钮上一版还因为少了一个定位属性、实际跑到屏幕外面去了，一并修掉 |
| **GBK 文档滚动时抖动** | 打开 `测试样例/GBK 编码样例.md`，慢慢滚到底部 | 上一版修掉的是「刚打开那一瞬间」的抖动，但**滚动过程中**还会抖。根因是每滚动一帧都要把全文所有标题挨个量一遍位置（长文档有上百个标题），这个动作会不停触发页面重排。这一版把这类计算限制到**每 50 毫秒最多一次**，肉眼就追不上了 |

---

## 七、诊断报告怎么看（剩下 3 个问题靠它定位）

按 `Ctrl + Shift + D` 复制出来的报告，每一行长这样：

```
[正常] 运行环境
    WebView2 140.0.1
```

方括号里是结论，只有三种：**正常** / **注意** / **异常**。看到「异常」的那几行就是线索。

| 报告里这一行 | 如果显示「异常」意味着什么 | 我这边下一步会怎么做 |
|---|---|---|
| **DOM 一致性（判决用）** | **最重要的一行**。它有几种不同的异常写法，对应完全不同的故障（详解见下） | 按下面的对照表处理 |
| 查找高亮（CSS Custom Highlight API） | 你这台电脑的 WebView2 **不支持**这项技术 | 换一种做法：改用 `<mark>` 标签把匹配文字包起来。代价是要改动页面结构，但功能能用 |
| 查找高亮现状 | `::highlight` 规则缺失，或开着查找栏却显示「已注册 0 组」 | 规则缺失 → 查主题初始化；注册 0 组 → 查上色逻辑 |
| **查找痕迹（跨会话）** | 命中 0 处、上色 0 次、最后注册 0 个，或「正文被换掉后重算 N 次」 | 见下面那一小节 |
| 大纲标题 id 匹配 | 大纲里有些标题，在正文**找不到对应的位置** | 结合上面「DOM 一致性」那行一起看，能分清是 id 没写进去、还是节点被换掉了 |
| 图片 1、2、3… | Content-Type 不是 `image/...`，或写着「加载失败」，或「开头是 `<!DOCTYPE`」 | 「开头是 HTML」说明请求**压根没拿到图片文件**，去查路径解析；只是类型标签不对，就去程序底层补一条类型对照表 |
| 当前文档统计 | 复制按钮的数量和预期**对不上** | 说明按钮挂载逻辑还有漏网的地方，我按差多少去查 |
| 阅读宽度 | 正文占比**明显不到 100%** | 说明第 24 项那个修复没生效，我再查样式的优先级 |

### 「初始化执行痕迹」那一行怎么读

这是最直接的一手证据（DOM 查询只能看到结果，痕迹能看到过程）：

| 报告里写的是 | 说明 | 我要改什么 |
|---|---|---|
| **一段记录都没有** | 那段初始化代码**一次都没执行** | 查它为什么没触发 |
| **被前置条件挡住了**（doc=false / 内容区 ref=false / 滚动容器 ref=false） | 三个条件里有东西是空的，代码在最开头就返回了 | 查对应的那个 ref 为什么没绑上 |
| 初始化：**没跑到** | 中间某一步出错中断了 | 查出错的那一步 |
| 初始化跑了、当时装了 9 个，但**之后被拆掉：是** | 按钮装好又被删 → 这就是「一个不剩」的直接原因 | 查为什么会立刻触发清理 |

### 「DOM 一致性」那一行怎么读

这是定位「点大纲不跳 + 复制按钮不显示」的关键，不同写法对应不同故障：

| 报告里写的是 | 说明 | 我要改什么 |
|---|---|---|
| 标题 21 个（**带 id 0 个**）；按钮宿主 **0 个** | 那段初始化代码**整体没执行** | 查为什么没执行，多半是某个前置条件没满足 |
| 标题 21 个（**带 id 21 个**）；按钮宿主 **0 个** | id 写进去了，但**按钮没装上** | 单独查按钮挂载那一段 |
| 标题带 id 齐全、按钮宿主也齐全，却依然看不见 | 都跑完了还是看不到 → **存在两个内容区**，操作落在一个上、显示的是另一个 | 查为什么会渲染出两个内容区 |
| `.markdown-body 2 个`，或「在文档树中：否」 | 内容区节点重复，或它脱离开了页面 | 同上 |

### 「页面抖动量化」那一行怎么读

**这一行是累计值，必须先滚动文档、再开面板才看得到数据。**

| 报告里写的是 | 说明 | 我要改什么 |
|---|---|---|
| **没记到任何布局位移** | 你开面板前没滚动，或真的没抖 | 确认一下是不是先滚过了再开的面板 |
| **累计位移 0.0xxx**（阈值 0.02 以下） | 有位移但很小，属正常范围 | 这个量级肉眼看不见，不用管 |
| **累计位移 0.0x ~ 0.x** | 明显抖动，肉眼能看见 | 看「位移最多的元素」是谁 |
| **最大单次 0.xx** | 某一下跳得很凶 | 重点查这个 |
| **位移最多的元素：`div.markdown-body ×12`** | 位移来自正文区 | 去查正文的高度为什么会变（图片撑开、字体、容器宽度等） |
| **位移最多的元素：`div.progress-bar`、`button.back-to-top`** | 位移来自界面控件而非正文 | 去改那一个控件的定位方式 |

> 这一行最有用的是**元素名**。以前只能听你描述「抖」，现在能直接看到是页面上哪个东西在动。

---

### 「查找痕迹（跨会话）」那一行怎么读

**这一行不用开着查找栏** —— 你关掉查找栏之后再来开面板，它还记得上一次搜索发生了什么。

| 报告里写的是 | 说明 | 我要改什么 |
|---|---|---|
| **Highlight API：支持** | 这台机器的浏览器内核支持高亮技术 | 那不高亮就是别的原因，继续往下看 |
| **Highlight API：不支持** | 内核不支持 → 高亮**根本不可能显示出来** | 按之前定好的方案，回退到 `<mark>` 标签包裹匹配文字 |
| **命中 0 处** | 摊平正文时没拿到文本，或关键词确实没匹配上 | 换个文档里肯定有的词再试一次；还是 0 就是摊平那一步有问题 |
| **命中 N 处，但最后注册 0 个** | 匹配算出来了，却一个高亮都没注册上去 | 查视口区间计算（`visibleIndices`） |
| **正文被换掉后重算 0 次** | 正文从未被换过 | 正常状态 |
| **正文被换掉后重算 N 次**（N > 0） | 正文被换过，查找已自动重算恢复 | 若反复出现，说明还有东西在替换正文，值得继续追 |
| **最近一次异常：CSS.highlights 不可用** | 内核不支持，且已被这一版的守卫挡住（不会崩） | 回退 `<mark>` 方案 |

---

> **你不用自己判断。** 把整份报告复制发给我，我照着改下一版就行。

**当前状态：8 项已全部处理完毕。** 下表是历史记录，保留以便追溯：

| 问题 | 修法 | 版本 |
|---|---|---|
| 阅读宽度 100% 未铺满 | 样式表硬编码宽度改由 CSS 变量驱动 | v0.2.3 |
| 复制按钮看不见 | 补 `pre` 的定位上下文 + 按钮改半透常驻 | v0.2.3 |
| 点大纲标题不跳 / 复制按钮没有 / 查找不高亮且跳转不对 / 滚动抖动 | **同一个根因**：React 19 按对象身份比较 `dangerouslySetInnerHTML`，每次重渲染都重写整篇正文。用 `useMemo` 钉住引用 | v0.2.9 |
| GBK 滚到底部整个窗口抖 | 滚动条震荡，加 `scrollbar-gutter: stable` | v0.2.10 |
| SVG / 大图不显示 | 与 v0.2.9 同一根因（正文被反复重建时 `<img>` 每次都要重新加载），随根因一并修复 | v0.2.9（确认于 v0.2.11） |
| 复制按钮挤进代码文字里 | `pre` 的 `padding-top` 从 12px 抬到 26px，按钮落在文字上方的独立区域 | v0.2.12 |
| 大文档打开慢（阅读不卡） | 已知性能特征，你已接受，不修 | — |

**装机实测 8 项至此全部闭环。**
---

## 三、把项目发到 GitHub（v0.2.12 首个公开版本）

8 项装机问题全部闭环，这是一个适合发布的状态。下面按顺序走就行 —— 任何一步停下来都可以，我都能帮你接下去。

### 〇、前置：你需要有的东西

| 项 | 怎么确认 |
|---|---|
| **GitHub 账号** | https://github.com —— 没就注册一个，五分钟 |
| **本机 git 已装** | 在 Git Bash 里跑 `git --version`，看到 `git version 2.x.x` 即可 |
| **SSH Key 或 PAT** | 二选一。SSH 更省事（push 不用每次输密码），PAT 是 GitHub 的个人访问令牌。本节**默认走 SSH**，PAT 在最末附 |

> 本项目目前是本地仓库（`.git/` 已建好，两个提交），下面所有命令都在 `E:/workbuddy/MD阅读器` 这个目录里跑。

---

### 一、在 GitHub 上建一个新仓库

1. 打开 https://github.com/new
2. **Repository name**：`md-reader`（或你喜欢的名字，下文都用这个）
3. **Description**（可选）：`基于 Tauri 2 + React 19 的本地 Markdown 阅读器`
4. **Public / Private**：选 **Public**（公开仓库，便于别人下载安装包；Private 也行，以后改 Public 容易）
5. **Add README / Add .gitignore / Add license**：**这三项都不要勾** —— 我们本地已经有了，加了反而冲突
6. 点 **Create repository**

创建完 GitHub 会跳到一个页面，最上面写着「Quick setup」之类。**别关这个页面** —— 下面的命令要用到里面的地址。

---

### 二、把本地仓库连上 GitHub

打开 Git Bash，`cd` 到项目根目录（**不是** `md-reader` 子目录）：

```bash
cd "E:/workbuddy/MD阅读器"

# 加一个远程仓库地址 —— 把下面的地址换成你 GitHub 页面里显示的那个
# SSH 形式：git@github.com:<你的用户名>/md-reader.git
# HTTPS 形式：https://github.com/<你的用户名>/md-reader.git
git remote add origin git@github.com:<你的用户名>/md-reader.git

# 验证：应能看到 origin 指向你刚填的地址
git remote -v
```

> **不知道该填 SSH 还是 HTTPS？** 看 GitHub 那个页面里两个按钮 —— 如果你之前配过 SSH Key，GitHub 会自动选 SSH，那行就直接复制；否则用 HTTPS 就行（下面的 SSH Key 一节会教你配）。

---

### 三、把第一个公开版本推上去

```bash
# 把 master 改名为 main（GitHub 新仓库默认分支名是 main，与本地一致更省事）
git branch -M main

# 把当前所有提交推到 origin/main
# —— 这是个不可逆操作，AI 默认禁止、需用户二次确认
git push -u origin main
```

`git push -u origin main` 是这一步里**唯一不可逆**的操作。我已经写好了两个 commit（v0.2.10 基线 + v0.2.11 收尾），v0.2.12 我等会儿也会提交。在你回「确认」之前我不会自己 push —— 这是项目里的老规矩。

**预期输出**：

```
Enumerating objects: 100, done.
Counting objects: 100% (100/100), done.
...
To github.com:<你的用户名>/md-reader.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

**报错怎么办**：

| 报错 | 原因 / 修法 |
|---|---|
| `Permission denied (publickey)` | SSH Key 没加，去 GitHub Settings → SSH and GPG keys 加上 |
| `remote: Repository not found` | 用户名或仓库名写错了，重新 `git remote set-url origin <正确地址>` |
| `failed to push some refs` | GitHub 上有 README 之类本地没有的文件 —— 这种情况**先确认是不是你刚建仓时勾了 Add README**，勾了就**删掉仓库重做**（点页面底部的 Delete this repository）；或者用 `git pull origin main --rebase` 把远端拉下来合上，但删仓重做更快 |
| `Could not resolve hostname` | 网络问题，重试一次 |

---

### 四、打标签（让 GitHub 知道这是 v0.2.12）

GitHub 用 git tag 来标记发布版本，这是后续「Releases」页面的数据来源。

```bash
# 给当前提交打一个带注释的标签
git tag -a v0.2.12 -m "v0.2.12：复制按钮顶位，8 项装机问题全闭环"

# 把标签推到 GitHub
git push origin v0.2.12
```

---

### 五、发一个 Release（带安装包下载链接）

这一节把 `MD阅读器-0.2.12-安装包.exe` 挂上去，让别人能在 GitHub 上直接下载。

**在网页上做**：

1. 打开 `https://github.com/<你的用户名>/md-reader/releases`
2. 点 **Draft a new release**（或 **Create a new release**）
3. **Choose a tag**：选刚才推上去的 `v0.2.12`
4. **Release title**：`v0.2.12 — 复制按钮顶位，8 项装机问题全闭环`
5. **Description**（描述区）写几句：

```markdown
## v0.2.12

复制按钮挪到代码块右上角，不再挡住正文。装机实测 8 项至此全部闭环。

**下载**：[MD阅读器-0.2.12-安装包.exe](./releases/download/v0.2.12/MD阅读器-0.2.12-安装包.exe)

**变更摘要**：
- 把代码块的 `padding-top` 抬到 26px，复制按钮永远落在文字上方的独立区域
- 按钮宿主 `z-index` 抬到 5，加深边框，半透改为更可见的实色
- 回归测试加 1 条「每个 pre/table 都挂上独立按钮宿主」（共 38 断言）

完整变更见 [`装好后怎么试.md`](./blob/main/装好后怎么试.md) 与 [`docs/PRD.md`](./blob/main/docs/PRD.md)。
```

6. **Attach binaries**：把 `E:/workbuddy/MD阅读器/MD阅读器-0.2.12-安装包.exe` 拖到这个框里（或点 "attach binaries by selecting them" 选择文件）。GitHub 会在后台自动校验。
7. 确认 **Set as the latest release**（默认就勾着的），点 **Publish release**

**完成后验证**：刷新 `https://github.com/<你的用户名>/md-reader/releases` —— 应该能看到 v0.2.12 这一行，下面挂着那个 exe 文件作为下载链接。

---

### 六、第一次别人进仓库看到的页面（可选但推荐）

GitHub 仓库首页如果空空的就有点冷清。建议加这几个文件（都不需要写很多）：

| 文件 | 在哪建 | 大概内容 |
|---|---|---|
| **`README.md`** | 仓库根 | 一句话介绍 + 一张截图 + 下载链接 + 「安装」三步走 + 「已知问题」列表 |
| **`LICENSE`** | 仓库根 | 选 MIT（最宽松，标 `MIT License` 加年份 + 你的名字即可） |
| **仓库描述 + Website** | 仓库页右上「About」右侧的齿轮 | 填 Description 与你想要的链接 |

README 的最简骨架可以这样（`E:/workbuddy/MD阅读器/README.md`，英文版更通用）：

```markdown
# MD Reader

A lightweight, native Markdown reader for Windows, built with Tauri 2 + React 19.

![screenshot](docs/screenshot.png)

## Install

1. Download `MD阅读器-0.2.12-安装包.exe` from the [Releases](../../releases) page
2. Run it; on SmartScreen warning click "More info" → "Run anyway"
3. Launch from the Start Menu

## Features

- GFM rendering (tables, task lists, strikethrough, autolinks, fenced code with syntax highlighting)
- Local-only, no upload anywhere
- Open `.md` by double-click in Explorer
- Outline sidebar · in-page search · reading width 70/80/90/100% · light/dark theme
- Built-in diagnostics panel (Ctrl+Shift+D) for triage

## Known limitations

- Large files (>5 MB) take a few seconds to open — render is not paginated
- No PDF export yet

## Development

See [`装好后怎么试.md`](./装好后怎么试.md) (Chinese) for full project history and changelog.
```

---

### 附：配 SSH Key（如果上面用了 HTTPS 想换 SSH）

打开 Git Bash：

```bash
# 1. 生成密钥（一路回车，passphrase 可空）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 复制公钥到剪贴板
cat ~/.ssh/id_ed25519.pub

# 3. 到 GitHub → Settings → SSH and GPG keys → New SSH key
#    Title 随便填，Key 粘贴上面 cat 出来的整段
# 4. 测试
ssh -T git@github.com
#   第一次会问"Are you sure you want to continue connecting" → yes
#   看到 "Hi <用户名>! You've successfully authenticated" 就成了

# 5. 把远程地址从 HTTPS 改成 SSH
git remote set-url origin git@github.com:<你的用户名>/md-reader.git
```

---

### 复盘 / 故障排查表

| 现象 | 原因 | 怎么办 |
|---|---|---|
| `git push` 要输 username + password | 用了 HTTPS 但没配 PAT | 配 Personal Access Token（GitHub Settings → Developer settings → PAT → 选 `repo` 权限 → 生成；push 时 username 填 GitHub 用户名、password 填 PAT 字符串） |
| `git push` 报 403 / 404 | 用户名或权限错 | 重新 `git remote set-url` |
| Release 页面上传 exe 后提示「Virus scan failed」 | GitHub 会自动扫所有上传文件 | 等几分钟后刷新；扫不出来就重传 |
| 别人下载安装包时被 SmartScreen 拦 | 没给 exe 签名 | 临时方案：让对方点「更多信息 → 仍要运行」；长期方案：花几百块买代码签名证书（每年），本项目目前没必要 |
| 后悔把某次提交推上去了 | 还没人 fork/clone 时可以强推覆盖 | `git reset --hard <上一版本commit>` 后 `git push --force`（**仅在公开仓库刚建、没别人参与时**才安全，否则用 `git revert`） |

---

发布流程到这里就结束了。**任何一步停下来、卡住、报错，把屏幕截图或终端输出贴给我，我接着帮你看。**
